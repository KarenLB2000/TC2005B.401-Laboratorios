<%- include('includes/head.ejs') %>

        <h1>Series: Manga</h1>
        <p>Actualmente esta es la lista de mangas que ofrecemos y publicamos bajo licencia:</p>

        <% if (mangas.length > 0) { %>
                <% for (let manga of mangas) { %>
                        <div class="card">
                                <div class="card-image waves-effect waves-block waves-light">
                                  <img class="activator" src="<%= manga.imagen %>">
                                </div>
                                <div class="card-content">
                                  <span class="card-title activator grey-text text-darken-4"><%= manga.titulo %><i class="material-icons right">more_vert</i></span>
                                  <p>Autor: <%= manga.autor %><br> Artista: <%= manga.artista %></p>
                                </div>
                                <div class="card-reveal">
                                  <span class="card-title grey-text text-darken-4"><%= manga.titulo %><i class="material-icons right">close</i></span>
                                  <p><%= manga.descripcion %></p>
                                </div>
                        </div>
                <% } %>
        <% } else { %>
                <h1>Actualmente, no manejamos una oferta de mangas.</h1>
        <% } %>  

        <a href="/series">Agregar una nueva serie.</a>
            
<%- include('includes/foot.ejs') %>